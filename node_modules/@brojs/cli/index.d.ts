import { i18n } from 'i18next';
import { History } from 'history';

type AppKey = string;
type FeatureName = string;
type Feature = { value: string, key: FeatureName };

/**
 * Returns full app config.
 */
export const getConfig: () => Record<string, string>;
/**
 * @param configKey - configKey;
 * 
 * @example
 * ```js
 * getConfigValue('my_app.api');
 * ```
 * 
 * @returns value of config key.
 */
export const getConfigValue: (configKey: string) => string;

/**
 * Returns all navigation values.
 *
 * @deprecated Use the new {@link getNavigation} base class instead.
 */
export const getNavigations: () => Record<string, string>;

/**
 * Returns all navigation values.
 */
export const getNavigation: () => Record<string, string>;

/**
 * Returns navigation values by key.
 * 
 * @param navKey - navigation key.
 * 
 * @deprecated Use the new {@link getNavigationValue} base class instead.
 */
export const getNavigationsValue: (navKey: string) => string;
/**
 * Returns navigation values by key.
 * 
 * @param navKey - navigation key.
 * 
 * @example
 * ```js
 * getNavigationValue('my_app.home');
 * getNavigationValue('my_app.main');
 * ```
 */
export const getNavigationValue: (navKey: string) => string;

/**
 * Get all features.
 * 
 */
export const getAllFeatures: () => Record<AppKey, Record<FeatureName, Feature>>;

/**
 * Get features of app.
 * 
 * @param pkgName - package name.
 * 
 * @example
 * ```js
 *  getFeatures('my_app');
 * ```
 */
export const getFeatures: (pkgName: string) => Record<FeatureName, Feature>;

/**
 * Get feature value.
 * 
 * @param pkgName - package name.
 * @param featureName - feature name.
 * @param defaultValue - default value if no any.
 * 
 * @example
 * ```js
 *  getFeatureValue('my_app', 'home');
 * ```
 */
export const getFeatureValue: <ReturnValue = string>(pkgName: string, featureName: string, defaultValue?: ReturnValue) => ReturnValue | undefined,

export const i18nextInitConfig: (i18next: i18n) => Promise<undefined>;
export const i18nextReactInitConfig: (i18next: i18n) => Promise<undefined>;
export const getHistory: () => History;

import { getModuleData } from "@brojs/utils";

const appsData = getModuleData().then(({ cleanName, version }) => ({
  [cleanName]: {
    version,
    name: cleanName,
  },
}));

export default appsData;

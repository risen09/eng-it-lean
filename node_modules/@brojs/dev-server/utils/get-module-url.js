import path from 'node:path';

const getModuleUrl = (moduleName, version, filePath) => {
    const modulePath = path.resolve('node_modules', moduleName, filePath);

    return modulePath;
}

export default getModuleUrl;

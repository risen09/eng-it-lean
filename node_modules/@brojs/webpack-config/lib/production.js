import { merge } from "webpack-merge";
import path from "path";

import getBaseConfig from "./base.js";

const getConfig = async () =>
  merge(await getBaseConfig(), {
    mode: "production",
    entry: {
      index: path.resolve(process.env.ENTRY),
    },
  });

export default getConfig;
